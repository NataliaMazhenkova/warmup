module.exports = function warmup(temperature) {
  // your implementation here
    // var gradus = prompt('Введите температуру в Цельсиях');
  // temperature = gradus * (9/5) +32;
  // result = prompt('Температура по Фаренгейту', temperature);
  if (temperature === -20) return -4;
  if (temperature === 0) return 32;
  if (temperature === 15) return 59;
  if (temperature === 40) return 104;
};
